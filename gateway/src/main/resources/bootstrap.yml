spring:
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        ip-address: 10.2.50.30
#        hostname: gateway

        port: 8080
        healthCheckPath: /health
      host: ${CONSUL_HOST}
      port: ${CONSUL_PORT}
      enabled: true