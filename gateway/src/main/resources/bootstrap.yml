spring:
  cloud:
    consul:
      discovery:
#        prefer-ip-address: true
#        ip-address: 192.168.80.104
        hostname: gateway

        port: 8080
        healthCheckPath: /health
      host: javatesting-consul
      port: 8500
      enabled: true