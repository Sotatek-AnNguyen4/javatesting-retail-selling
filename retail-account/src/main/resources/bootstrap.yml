spring:
  cloud:
    consul:
      discovery:
        prefer-ip-address: true
        ip-address: 10.2.51.80
#        hostname: retail-account

        port: 8083
        healthCheckPath: /health
      host: 127.0.0.1
      port: 8500
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:13306/retailselling_retailaccount
    username: root
    password: 1
    driver-class-name: com.mysql.jdbc.Driver